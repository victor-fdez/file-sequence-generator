machine:
  node:
    version: 6.9.1
  timezone: America/Denver
dependencies:
  pre:
    - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
test:
deployment:
  develop:
    branch: master
    commands:
      - npm publish

