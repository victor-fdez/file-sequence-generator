machine:
  node:
    version: 6.9.1
  timezone: America/Denver
dependencies:
  pre:
    - echo -e "$NPM_USER\n$NPM_PASS\n$NPM_EMAIL" | npm login
    - npm install
test:
  override:
    - npm run test 
deployment:
  develop:
    branch: master
    commands:
      - npm publish

